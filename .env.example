# Supabase project credentials
# Copy to .env and fill in from your Supabase dashboard (Project Settings > API).
# Note: VITE_ vars are inlined into the client bundle and are public by design.
# Security depends on Row Level Security policies, not on hiding these values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
